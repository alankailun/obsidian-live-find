{
  "name": "obsidian-live-find",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/plugin.js --bundle --platform=node --format=cjs --external:obsidian --outfile=main.js",
    "check": "npm run build && node --check main.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
